-- generated by cds-compiler version 2.11.4
COLUMN TABLE my_bookshop_Genres (
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  ID NVARCHAR(36) NOT NULL,
  parent_ID NVARCHAR(36),
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN my_bookshop_Genres AS parent ON (parent.ID = parent_ID),
  MANY TO MANY JOIN my_bookshop_Genres AS children ON (children.parent_ID = ID)
)